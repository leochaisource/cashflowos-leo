feat(projects): add Savwee Closer; Dr Tariq moves to monitoring + invoicing

- Savwee Closer added as a work project with one open step, "Follow up
  remaining leads". No deadline was given, so none was invented — it shows as
  "no deadline set" and the brief nudges once to date it.

- Dr Tariq: cleanup is done and verified, so the project moves to `monitoring`
  and its TARGET DATE IS CLEARED. That matters — the delivery date (18 Aug) was
  met, and leaving it set would keep painting a finished piece of work red as
  "overdue" forever. The live deadline now belongs where it should: the invoice.

- The task holding a whole sentence as its title ("Done and completed. Just need
  to monitor... invoice to be collected in 1 weeks time") is rewritten as an
  action — "Collect invoice from Dr Tariq", due 31 Aug — rather than left as a
  status note masquerading as a to-do, or duplicated by a second task.

- `monitoring` and `negotiation` were both in use with no pill styling, so they
  rendered as anonymous grey. Monitoring joins the blue in-progress group,
  negotiation the amber attention group.

No invoice RECORD was created: no amount was given, and a cash_in row of RM 0
would put a fake zero into Cash In and "Who owes me". The task carries it until
the number is known.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
