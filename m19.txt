feat(clients): remove Dianna from the registry — the engagement ended

Removing the entry IS the archive. Everything visible keys off the registry —
dashboard card, sidebar, morning brief, bot tools, both crons — so one deletion
removes her everywhere at once, and her history stays exactly where it is:
ad_daily, competitor_ads and records rows remain in Supabase under 'dianna-nlp',
read by nothing while no registry entry carries that id. Restoring the entry
from git history brings her back intact.

Nothing else spends on her account any more: her 3 Adyntel keywords/day stop,
and her Meta env vars (META_AD_ACCOUNT_ID / META_ACCESS_TOKEN) are now unused —
the token should be revoked in Business Manager, not merely unset, since it was
once pasted into chat.

Stragglers swept: the replay tool's default client, the bot tool examples and
the sync-ads doc comment no longer name her. The Kestrel demo seeder still
copies stored 'dianna-nlp' competitor ads — they remain in the database, so it
keeps working.

Verified: a full sync run now iterates claude-malaysia + starcity-global only.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
